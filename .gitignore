# Database — synced separately via Google Drive / cloud, NOT via git
*.db
*.db-shm
*.db-wal

# Local config — stores per-machine DB path, not shared via git
incubation_config.json

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Desktop shortcut / icon artefacts
*.ico
*.lnk
*.ps1

# VS Code / editor folders
.vscode/
.idea/

# OS files
Thumbs.db
.DS_Store
